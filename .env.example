# Phase 2 provides the client and migrations but does not connect a real project.
# Copy this file to .env.local only when you are ready for the manual setup.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
